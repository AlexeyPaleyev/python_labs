# Дано текст: у першому рядку записано число рядків, далі йдуть рядки. Визначте, скільки
# різних слів міститься у цьому тексті.
# Словом вважається послідовність непробільних символів, що йдуть підряд, слова
# розділені одним або більшим числом пробілів або символами кінця рядка.

n = int(input())
A = set()
for i in range(n):
    A.update(set(input().split()))
print(len(A))
