# Дано дійсне позитивне число a і негативне число n. Обчисліть an не використовуючи
# цикли, зведення у ступінь через ** та функцію math.pow(), а використовуючи рекурентне
# співвідношення an=a⋅an-1.
# Рішення оформіть як функції power(a, n).


def power(d, st):
    if st == 0:
        ret = 1
    else:
        ret = d * power(d, n - 1)
    return (ret)


a = float(input())
n = int(input())
